#include <stdio.h>
#include <stdlib.h>

int main()
{
    int v[5];
    for (int i = 0; i < 5; i++)
    {
        v[i] = i + 10;
    }
    int min, max;
    mm(v, 5, &min, &max);
    printf("p", )
    return 0;
}
